# Changelog\n\nTodas as mudanças relevantes do projeto (resumo condensado)\n\n## Unreleased\n\n### Features\n\n- Representação VRP e parser CSV (`src/ga/representation.py`).\n- Função de fitness com Haversine e penalidades por capacidade/prioridade (`src/ga/fitness.py`).\n- Operadores genéticos: `relocate`, `swap`, `2-opt`, e `crossover_vrp` (`src/ga/operators.py`).\n- Heurísticas de inicialização: `nearest_neighbor_init`, `clarke_wright_savings` (`src/ga/initialization.py`).\n- População e seleção (`tournament`, `roulette`) (`src/ga/population.py`).\n- Engine GA e runner de exemplo (`src/ga/engine.py`, `scripts/run_ga_example.py`).\n- Export e visualização: GeoJSON, mapa HTML e `results.csv` (`src/viz/map.py`, `scripts/export_artifacts.py`).\n- Integração LLM (OpenAI adapter + fallback) e geração de `instruction.txt` (`src/llm/adapter.py`, `scripts/generate_instructions.py`).\n- API FastAPI com endpoints `/optimize`, `/jobs/{id}`, `/routes/{id}`, `/instructions/{id}` (`src/api/app.py`).\n- Scripts utilitários: `scripts/run_local.sh`, `build_wheelhouse.sh`, `install_from_wheelhouse.sh`, `install_system_deps.sh`.\n\n### Tests\n\n- Testes unitários para representação, fitness, operadores, inicialização e integração API (`tests/ga/`, `tests/api/`).\n\n### Docs\n\n- Documentação detalhada adicionada/atualizada: `README.md`, `docs/*` (design_repr, implementation_notes, fitness, operators, initialization, runtime, output_formats, llm_integration, api).\n\n## Notes\n\n- Para reprodução local, veja `README.md` (Quickstart) e `docs/runtime.md`.\n- Alguns pacotes geoespaciais podem exigir dependências de sistema (veja `scripts/install_system_deps.sh`).\n\n*** End Patch
