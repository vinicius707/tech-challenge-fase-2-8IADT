/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // basePath opcional: descomente se o frontend for servido em subpath
  // basePath: '/frontend',
}

module.exports = nextConfig
